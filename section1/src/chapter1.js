// 자바스크립트 변수 규칙
// 1. 변수명에는 $, _ 를 제외한 다른기호는 사용할 수 없다.
// 2. 변수명의 맨앞에는 숫자를 사용할 수 없다.
// 3. 변수명에는 예약어를 사용할 수 없다.

let name = "효빈";
name = "재형"; // 수정가능
console.log(name);

// 카멜 표기법
let userInfo;
let isTrueOrFalse;

// 상수 변하지 않는 값을 저장하는 저장소
const name1 = "효빈";
// name = "재형";   // 수정불가능
